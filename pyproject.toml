[project]
name = "tasky-cli"
version = "0.1.0"
description = "A CLI based AI task manager agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.3.0",
    "python-dotenv>=1.1.0",
]
